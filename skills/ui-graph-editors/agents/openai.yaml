interface:
  display_name: UI Graph Editors
  short_description: Preserve graph identity and usable non-canvas workflows
  default_prompt: Use $ui-graph-editors for this task. Preserve repository instructions and supported
    versions. Inspect the relevant source, use only appropriate tools, and report actual evidence and
    unrun checks.
policy:
  allow_implicit_invocation: true

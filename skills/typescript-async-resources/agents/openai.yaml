interface:
  display_name: TypeScript Async Resources
  short_description: Bound async work and prevent stale results from taking ownership
  default_prompt: Use $typescript-async-resources for this task. Preserve repository instructions and
    supported versions. Inspect the relevant source, use only appropriate tools, and report actual evidence
    and unrun checks.
policy:
  allow_implicit_invocation: true

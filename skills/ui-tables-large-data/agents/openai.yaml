interface:
  display_name: UI Tables and Large Data
  short_description: Keep large datasets responsive and semantically correct
  default_prompt: Use $ui-tables-large-data for this task. Preserve repository instructions and supported
    versions. Inspect the relevant source, use only appropriate tools, and report actual evidence and
    unrun checks.
policy:
  allow_implicit_invocation: true

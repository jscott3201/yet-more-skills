interface:
  display_name: UI Security Boundaries
  short_description: Preserve browser trust boundaries and backend authority
  default_prompt: Use $ui-security-boundaries for this task. Preserve repository instructions and supported
    versions. Inspect the relevant source, use only appropriate tools, and report actual evidence and
    unrun checks.
policy:
  allow_implicit_invocation: true

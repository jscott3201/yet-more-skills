interface:
  display_name: Vite Build and Tooling
  short_description: Validate the actual Vite pipeline and production artifact
  default_prompt: Use $vite-build-tooling for this task. Preserve repository instructions and supported
    versions. Inspect the relevant source, use only appropriate tools, and report actual evidence and
    unrun checks.
policy:
  allow_implicit_invocation: true

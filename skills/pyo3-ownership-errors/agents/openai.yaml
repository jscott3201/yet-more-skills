interface:
  display_name: PyO3 Ownership and Errors
  short_description: Keep native ownership and Python contracts aligned.
  default_prompt: Use $pyo3-ownership-errors for this task. Follow repository instructions,
    inspect the relevant code and environment, and report verified results and remaining
    gaps.
policy:
  allow_implicit_invocation: true

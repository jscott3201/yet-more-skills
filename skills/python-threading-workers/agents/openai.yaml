interface:
  display_name: Python Threads and Workers
  short_description: Bound worker concurrency and protect shared state
  default_prompt: Use $python-threading-workers for this task. Follow repository instructions,
    inspect the relevant code and environment, and report verified results and remaining
    gaps.
policy:
  allow_implicit_invocation: true

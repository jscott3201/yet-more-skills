interface:
  display_name: Svelte Components and Runes
  short_description: Use Svelte reactivity with clear component ownership
  default_prompt: Use $svelte-components-runes for this task. Preserve repository instructions and supported
    versions. Inspect the relevant source, use only appropriate tools, and report actual evidence and
    unrun checks.
policy:
  allow_implicit_invocation: true

interface:
  display_name: "Rust Python Bindings"
  short_description: "Validate Python ownership, threading, and async bridges"
  default_prompt: "Use $rust-python-bindings for this task. Follow repository instructions, inspect the relevant code, and report verified results and remaining gaps."
policy:
  allow_implicit_invocation: true

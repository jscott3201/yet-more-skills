interface:
  display_name: PR Review and Validation
  short_description: Practical pr review and validation guidance
  default_prompt: Use $pr-gate-loop for the requested task, keeping its scope and actual evidence explicit.
policy:
  allow_implicit_invocation: false

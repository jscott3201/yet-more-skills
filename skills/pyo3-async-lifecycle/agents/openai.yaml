interface:
  display_name: PyO3 Async Lifecycle
  short_description: Coordinate Tokio, asyncio, cancellation, and teardown.
  default_prompt: Use $pyo3-async-lifecycle for this task. Follow repository instructions,
    inspect the relevant code and environment, and report verified results and remaining
    gaps.
policy:
  allow_implicit_invocation: true

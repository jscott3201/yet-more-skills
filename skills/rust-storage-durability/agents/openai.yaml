interface:
  display_name: "Rust Storage and Durability"
  short_description: "Protect durability, recovery, and index consistency"
  default_prompt: "Use $rust-storage-durability for this task. Follow repository instructions, inspect the relevant code, and report verified results and remaining gaps."
policy:
  allow_implicit_invocation: true

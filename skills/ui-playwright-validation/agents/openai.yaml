interface:
  display_name: Playwright Browser Validation
  short_description: Prove browser workflows without replacing behavior with sleeps
  default_prompt: Use $ui-playwright-validation for this task. Preserve repository instructions and supported
    versions. Inspect the relevant source, use only appropriate tools, and report actual evidence and
    unrun checks.
policy:
  allow_implicit_invocation: true

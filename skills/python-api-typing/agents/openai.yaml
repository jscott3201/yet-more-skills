interface:
  display_name: Python APIs and Typing
  short_description: Keep Python APIs, annotations, and runtime behavior aligned
  default_prompt: Use $python-api-typing for this task. Follow repository instructions,
    inspect the relevant code and environment, and report verified results and remaining
    gaps.
policy:
  allow_implicit_invocation: true

interface:
  display_name: Resolve Material Owner Decisions
  short_description: Practical resolve material owner decisions guidance
  default_prompt: Use $owner-decision-gate for the requested task, keeping its scope and actual evidence explicit.
policy:
  allow_implicit_invocation: true

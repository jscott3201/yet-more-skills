interface:
  display_name: Building Operations UI Workflows
  short_description: Keep operator decisions grounded in real system state
  default_prompt: Use $ui-operational-workflows for this task. Preserve repository instructions and supported
    versions. Inspect the relevant source, use only appropriate tools, and report actual evidence and
    unrun checks.
policy:
  allow_implicit_invocation: true

interface:
  display_name: React Performance
  short_description: Profile React bottlenecks before choosing optimizations
  default_prompt: Use $react-performance for this task. Preserve repository instructions and supported
    versions. Inspect the relevant source, use only appropriate tools, and report actual evidence and
    unrun checks.
policy:
  allow_implicit_invocation: true

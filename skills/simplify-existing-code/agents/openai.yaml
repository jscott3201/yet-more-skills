interface:
  display_name: Simplify Existing Code
  short_description: Practical simplify existing code guidance
  default_prompt: Use $simplify-existing-code for the requested task, keeping its scope and actual evidence explicit.
policy:
  allow_implicit_invocation: false

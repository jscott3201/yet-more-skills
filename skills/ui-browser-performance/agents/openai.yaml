interface:
  display_name: Browser UI Performance
  short_description: Measure end-to-end UI cost before tuning individual layers
  default_prompt: Use $ui-browser-performance for this task. Preserve repository instructions and supported
    versions. Inspect the relevant source, use only appropriate tools, and report actual evidence and
    unrun checks.
policy:
  allow_implicit_invocation: true

interface:
  display_name: "Rust Performance Engineering"
  short_description: "Turn measured Rust bottlenecks into defensible improvements"
  default_prompt: "Use $rust-performance for this task. Follow repository instructions, inspect the relevant code, and report verified results and remaining gaps."
policy:
  allow_implicit_invocation: true

interface:
  display_name: Coordinate a Bounded Change
  short_description: Practical coordinate a bounded change guidance
  default_prompt: Use $orchestrate for the requested task, keeping its scope and actual evidence explicit.
policy:
  allow_implicit_invocation: false

interface:
  display_name: Parallel Work Lanes
  short_description: Practical parallel work lanes guidance
  default_prompt: Use $parallel-portfolio for the requested task, keeping its scope and actual evidence explicit.
policy:
  allow_implicit_invocation: false

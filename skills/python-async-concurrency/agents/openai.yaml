interface:
  display_name: Python Async and Cancellation
  short_description: Preserve async ownership, deadlines, and cleanup
  default_prompt: Use $python-async-concurrency for this task. Follow repository instructions,
    inspect the relevant code and environment, and report verified results and remaining
    gaps.
policy:
  allow_implicit_invocation: true

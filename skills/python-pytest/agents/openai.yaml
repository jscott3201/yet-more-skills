interface:
  display_name: Python Pytest
  short_description: Run the intended tests without false-green results
  default_prompt: Use $python-pytest for this task. Follow repository instructions,
    inspect the relevant code and environment, and report verified results and remaining
    gaps.
policy:
  allow_implicit_invocation: true

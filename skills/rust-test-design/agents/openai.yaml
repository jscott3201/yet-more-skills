interface:
  display_name: "Rust Behavioral Testing"
  short_description: "Build tests that challenge behavior rather than repeat it"
  default_prompt: "Use $rust-test-design for this task. Follow repository instructions, inspect the relevant code, and report verified results and remaining gaps."
policy:
  allow_implicit_invocation: true

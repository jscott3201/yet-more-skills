interface:
  display_name: Vitest and Component Testing
  short_description: Test behavior with the right environment and real assertions
  default_prompt: Use $ui-vitest-testing for this task. Preserve repository instructions and supported
    versions. Inspect the relevant source, use only appropriate tools, and report actual evidence and
    unrun checks.
policy:
  allow_implicit_invocation: true

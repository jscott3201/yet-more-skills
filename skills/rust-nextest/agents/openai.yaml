interface:
  display_name: "Rust Nextest"
  short_description: "Run precise, resource-aware Rust tests without false greens"
  default_prompt: "Use $rust-nextest for this task. Follow repository instructions, inspect the relevant code, and report verified results and remaining gaps."
policy:
  allow_implicit_invocation: true

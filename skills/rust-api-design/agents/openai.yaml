interface:
  display_name: "Rust API and Ownership"
  short_description: "Design clear ownership, types, traits, and error contracts"
  default_prompt: "Use $rust-api-design for this task. Follow repository instructions, inspect the relevant code, and report verified results and remaining gaps."
policy:
  allow_implicit_invocation: true

interface:
  display_name: Python Packaging and Environments
  short_description: Keep environments reproducible and artifacts installable
  default_prompt: Use $python-packaging-environments for this task. Follow repository
    instructions, inspect the relevant code and environment, and report verified results
    and remaining gaps.
policy:
  allow_implicit_invocation: true

interface:
  display_name: Source Verification
  short_description: Practical source verification guidance
  default_prompt: Use $source-verification for the requested task, keeping its scope and actual evidence explicit.
policy:
  allow_implicit_invocation: true

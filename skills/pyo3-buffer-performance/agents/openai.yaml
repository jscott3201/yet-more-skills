interface:
  display_name: PyO3 Buffers and Performance
  short_description: Measure boundary cost without unsafe zero-copy shortcuts.
  default_prompt: Use $pyo3-buffer-performance for this task. Follow repository instructions,
    inspect the relevant code and environment, and report verified results and remaining
    gaps.
policy:
  allow_implicit_invocation: true

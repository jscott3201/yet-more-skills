interface:
  display_name: "Rust Workflow"
  short_description: "Choose a focused Rust workflow and validation scope"
  default_prompt: "Use $rust-workflow for this task. Follow repository instructions, inspect the relevant code, and report verified results and remaining gaps."
policy:
  allow_implicit_invocation: false

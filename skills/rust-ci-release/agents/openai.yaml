interface:
  display_name: "Rust CI and Release Validation"
  short_description: "Keep PR checks lean and release evidence native and complete"
  default_prompt: "Use $rust-ci-release for this task. Follow repository instructions, inspect the relevant code, and report verified results and remaining gaps."
policy:
  allow_implicit_invocation: false

interface:
  display_name: Evidence-Based PR Review
  short_description: Practical evidence-based pr review guidance
  default_prompt: Use $immutable-pr-review for the requested task, keeping its scope and actual evidence explicit.
policy:
  allow_implicit_invocation: true

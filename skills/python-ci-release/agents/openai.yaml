interface:
  display_name: Python CI and Release
  short_description: Keep Python PR gates lean and release evidence real.
  default_prompt: Use $python-ci-release for this task. Follow repository instructions,
    inspect the relevant code and environment, and report verified results and remaining
    gaps.
policy:
  allow_implicit_invocation: false

interface:
  display_name: Tracer-Bullet Planning
  short_description: Practical tracer-bullet planning guidance
  default_prompt: Use $tracer-bullet-planning for the requested task, keeping its scope and actual evidence explicit.
policy:
  allow_implicit_invocation: true

interface:
  display_name: TypeScript UI Workflow
  short_description: Ground frontend work in its framework, contracts, and checks
  default_prompt: Use $typescript-workflow for this task. Preserve repository instructions and supported
    versions. Inspect the relevant source, use only appropriate tools, and report actual evidence and
    unrun checks.
policy:
  allow_implicit_invocation: false
